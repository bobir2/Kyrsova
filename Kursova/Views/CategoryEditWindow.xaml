<Window x:Class="Kursova.Views.CategoryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Категорія"
        Height="220" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Назва *" />
            <TextBox x:Name="NameBox" />
            <TextBlock Text="Опис" Margin="0,8,0,0" />
            <TextBox x:Name="DescriptionBox" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Скасувати" Click="Cancel_Click" Style="{StaticResource SecondaryButtonStyle}" Width="100" />
            <Button Content="Зберегти" Click="Save_Click" Width="100" />
        </StackPanel>
    </Grid>
</Window>
